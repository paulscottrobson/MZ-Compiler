; @word screen.mode.48k

SetScreenMode48kSpectrum:
		push 	de
		push 	hl
		call 	SetMode_Spectrum48k
		ld		(SIScreenDriver),de
		ld 		a,l
		ld 		(SIScreenWidth),a
		ld 		a,h
		ld 		(SIScreenHeight),a
		ld 		hl,0
		ld 		(IOScreenPosition),hl
		pop 	hl
		pop 	de
		ret
		

